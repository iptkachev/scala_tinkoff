package main.scala.collections

object kOrder {
  def main(args: Array[String]) {
    def partition(list: List[Int], start: Int, end: Int, k: Int): Int = {

    }

    def kOrder(list: List[Int], k: Int): Int = {
      val partition_element = Nil
      if (k == partition_element)
    }
  }
}
