"""
  | Nothing - базовый trait для всех типов скалы, поэтому можно возвращать любой тип (например бросать exception) и его
  |
  | Null - почти как Nothing, только не является предком примитивов
  |
  | Nil - это объет, например им можно обозначить пустой список
  |
  | None - это тоже не тип, а объект None. Тип у этого объекта None.type.
  |   Объект None - один из двух наследников класса Option и используется как пустой вариант Option.
  |""".stripMargin

